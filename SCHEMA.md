# SCHEMA

本库的结构约定。任何 agent 在读写本库之前必须先读完本文件。

## 分层

| 目录 | 放什么 |
|---|---|
| `raw/` | 原始材料原样存档。只追加，不改写。 |
| `entity/` | 具体事物：人、组织、项目、论文、工具、产品、事件 |
| `concepts/` | 抽象的概念、方法、原理、模式 |
| `comparisons/` | 两个及以上对象的横向对比 |
| `queries/` | 我问过的问题，以及当时得出的结论 |

判断放哪一层的标准：能指着说"这个东西"的进 `entity/`，只能解释不能指的进 `concepts/`。

## 命名

kebab-case，一个文件一个主题，扩展名 `.md`。

- 普通笔记：`llm-wiki.md`、`andrej-karpathy.md`
- `raw/` 下加日期前缀：`2026-07-25-karpathy-llm-wiki-gist.md`
- `comparisons/` 用 `a-vs-b.md`
- `queries/` 用问题的短语形式：`why-flash-attention-is-fast.md`

中文文件名可以用，但同一个概念只能有一个名字，别中英文各建一份。

## frontmatter

每个文件开头：

```yaml
---
title: 人类可读的标题
type: raw | entity | concept | comparison | query
created: YYYY-MM-DD
updated: YYYY-MM-DD
tags: [标签, 标签]
sources: [URL 或 [[raw/...]]]
---
```

## 链接

用 `[[file-name]]`，不带路径、不带扩展名。仅当同名文件出现在多个目录时（比如各目录下的 `README`），才用目录限定：`[[raw/README]]`。

**链接到还不存在的文件是允许且鼓励的**——它标记了一个值得写但还没写的坑，不是错误。定期扫描这些悬空链接，就是选题清单。

## 正文规范

- 第一句话是定义或结论，不要铺垫
- 事实和推测分开：推测写 `(推测)`，存疑写 `(存疑：原因)`
- 引用原始材料时链到 `[[raw/...]]`，不要复制大段原文
- 没有把握的日期、数字、人名，标注来源；查不到就写"未核实"

## 录入新材料的流程

agent 收到一份新材料时按顺序执行：

1. 原文存入 `raw/`，frontmatter 里记下来源 URL 和获取日期
2. 抽取其中的实体和概念。新的建文件；已存在的补充内容并更新 `updated`
3. **回头扫描相关的旧笔记**，把新出现的关联补成 `[[链接]]`——这一步是整个库产生复利的地方，不要跳过
4. 更新 `index.md` 的对应分区
5. 在 `log.md` 末尾追加一行，**其中提到的每个文件都写成 `[[链接]]`**，不能只写文件名——日志要能直接跳回当时动过的内容

## 不要做的事

- 不要改写或"整理" `raw/` 下的原文
- 不要为一两句话建独立文件，先并入已有笔记，等它长大再拆出去
- 不要建只有标题没有内容的占位文件（悬空的 `[[链接]]` 比空文件好）
- 不要在 `index.md` 里写内容，它只是目录
- 不要为了让笔记显得完整而补充你不确定的内容
